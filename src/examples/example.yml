description: >
  Example kubeval orb.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    kubevalorb: kiddo3/kubevalorb@1.0.0
  workflows:
    use-my-orb:
      jobs:
        - kubevalorb/install-kubeval
        - kubevalorb/kubeval:
            directories: src/tests/manifests
